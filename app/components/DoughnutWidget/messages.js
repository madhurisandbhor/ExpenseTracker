/*
 * DoughnutWidget Messages
 *
 * This contains all the text for the DoughnutWidget component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DoughnutWidget';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DoughnutWidget component!',
  },
});
