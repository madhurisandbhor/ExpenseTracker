/*
 * ModalDialog Messages
 *
 * This contains all the text for the ModalDialog component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ModalDialog';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ModalDialog component!',
  },
});
