// import { selectLatestExpenseListDomain } from '../selectors';

describe('selectLatestExpenseListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
