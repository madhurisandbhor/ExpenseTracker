// import { selectStatisticsContainerDomain } from '../selectors';

describe('selectStatisticsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
