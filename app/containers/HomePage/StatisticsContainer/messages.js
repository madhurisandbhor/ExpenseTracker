/*
 * StatisticsContainer Messages
 *
 * This contains all the text for the StatisticsContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StatisticsContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatisticsContainer container!',
  },
});
