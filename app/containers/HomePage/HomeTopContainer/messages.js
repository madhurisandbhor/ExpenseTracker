/*
 * HomeTopContainer Messages
 *
 * This contains all the text for the HomeTopContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HomeTopContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeTopContainer container!',
  },
});
