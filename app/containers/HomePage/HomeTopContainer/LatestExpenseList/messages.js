/*
 * LatestExpenseList Messages
 *
 * This contains all the text for the LatestExpenseList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LatestExpenseList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LatestExpenseList container!',
  },
});
