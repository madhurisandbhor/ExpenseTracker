// import { selectHomeTopContainerDomain } from '../selectors';

describe('selectHomeTopContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
