/*
 * ConnectionForm Messages
 *
 * This contains all the text for the ConnectionForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ConnectionForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ConnectionForm container!',
  },
});
